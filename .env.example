# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# JWT and Authentication Secrets
JWT_SECRET="secure_jwt_access_secret_key_change_in_production_2026"
JWT_REFRESH_SECRET="secure_jwt_refresh_secret_key_change_in_production_2026"
SESSION_SECRET="secure_session_encryption_key_2026"

# Optional Email SMTP credentials for OTP (Mock/Preview fallback active when unconfigured)
SMTP_HOST="smtp.mailtrap.io"
SMTP_PORT="2525"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@sms-portal.edu"

# Optional MongoDB Atlas connection URI (Default: Persistent JSON/Embedded Mongoose-compatible DB)
MONGODB_URI=""

